# Раздел ЕГЭ в боте

В меню ученика есть пункт **«📚 Раздел ЕГЭ»** — 27 заданий по информатике. По нажатию на номер показывается заголовок и **две кнопки**:
- **«📋 Показать задание»** — бот присылает фото с условием задачи (`task_image`);
- **«📎 Показать решение»** — присылает разбор: если решение — код (в `example_solution`), то **текстом**; иначе — фото (`solution_image`).

## Откуда брать контент

Материалы можно брать с сайта **https://code-enjoy.ru/** (курс ЕГЭ по информатике):

- https://code-enjoy.ru/courses/kurs_ege_po_informatike/
- Разделы по заданиям (1–11, 13–16, 19–21, 22–27 и т.д.)

Для каждого задания в БД хранятся:

- **title** — короткое название (например, «Графы, таблица дорог»)
- **task_image** — фото с условием: путь `ege_images/N_task.png` или URL
- **solution_image** — фото с решением: путь `ege_images/N_solution.png` или URL
- **example_solution** — если решение задаётся **кодом**, сюда вставляют текст кода — бот выведет его текстом по кнопке «Показать решение»
- **explanation** — краткое объяснение (по желанию)
- **source_url** — ссылка на страницу (по желанию)

## Как заполнить БД

### Вариант 1: JSON-файл

1. Скопируй структуру из `ege_tasks_example.json`.
2. Создай файл `ege_tasks.json` в корне проекта и заполни все 27 заданий (или часть), копируя текст с code-enjoy.ru.
3. Локально выполни:
   ```bash
   python load_ege_from_json.py ege_tasks.json
   ```
4. Файл БД обновится. На Railway нужно либо залить обновлённую БД (Volume), либо один раз выполнить скрипт на сервере (через run command / SSH), если у тебя есть доступ.

### Задания 1–6 с готовыми скринами

В папке `ege_images/` уже лежат файлы `1_task.png`, `1_solution.png` … `6_task.png`, `6_solution.png`. Чтобы прописать их в БД и код решений для заданий 2, 5, 6, выполни один раз:

```bash
python set_ege_images_1_6.py
```

(нужен установленный `aiosqlite`, обычно уже есть в окружении бота.)

### Вариант 2: Добавить в бота кнопку «Редактировать задание ЕГЭ» (только для админа)

Можно потом добавить в админ-меню возможность вводить/обновлять задание по номеру (текст + пример решения), чтобы не трогать JSON и БД вручную.

## Формат JSON

```json
[
  {
    "task_number": 1,
    "title": "Кодирование информации",
    "explanation": "Краткое объяснение задания...",
    "example_solution": "Код или текст решения. Если код — бот выведет текстом.",
    "source_url": "https://code-enjoy.ru/...",
    "solution_image": "ege_images/1_solution.png",
    "task_image": "ege_images/1_task.png"
  }
]
```

Поля **title**, **explanation**, **example_solution**, **source_url**, **solution_image**, **task_image** могут быть пустыми. Номер **task_number** — от 1 до 27. Файлы клади в `ege_images/` (см. `ege_images/README.md`).
